version: "3"

services:
  # Applications
  entrypoint:
    image: golang:1.15.6
    working_dir: /app
    volumes:
        - ./apps/entrypoint:/app
    ports:
      - 8080:8080
    command: make run

  enrichment:
    image: golang:1.15.6
    working_dir: /app
    volumes:
        - ./apps/enrichment:/app
    ports:
      - 8081:8080
    command: make run

  response-builder:
    image: golang:1.15.6
    working_dir: /app
    volumes:
        - ./apps/response-builder:/app
    ports:
      - 8082:8080
    command: make run

  # Datastores
  # Monitoring
