apiVersion: v1
kind: Service
metadata:
  name: {{ include "entrypoint.fullname" . }}
  labels:
    {{- include "entrypoint.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "entrypoint.selectorLabels" . | nindent 4 }}
