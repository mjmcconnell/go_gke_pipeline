apiVersion: v1
kind: Service
metadata:
  name: {{ include "enrichment.fullname" . }}
  labels:
    {{- include "enrichment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "enrichment.selectorLabels" . | nindent 4 }}
