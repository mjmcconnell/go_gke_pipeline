apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enrichment.fullname" . }}-test-connection"
  labels:
    {{- include "enrichment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enrichment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
