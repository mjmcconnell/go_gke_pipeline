apiVersion: v1
kind: Service
metadata:
  name: {{ include "response-builder.fullname" . }}
  labels:
    {{- include "response-builder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "response-builder.selectorLabels" . | nindent 4 }}
