apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "response-builder.fullname" . }}-test-connection"
  labels:
    {{- include "response-builder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "response-builder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
